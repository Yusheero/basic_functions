#Функция сортировки каждой цифры в числе от большего к меньшему
def descending_order(num):
    return int("".join(sorted(str(num), reverse=True)))
    
    #str - преобразование к строке
    #sorted - сортировка строки
    #join - метод объединения всех элементов списка с разделителем ""
    #reverse=True - (параметр) по умолчанию сортировка выполняется по возрастанию. Если указать reverse=True, то можно отсортировать по убыванию.



#Функция суммы всех целых чисел от 1 до num
def summation(num):
    return sum(range(1,num+1))
    
    #range - генерирует список чисел от 1 до num+1
    #sum - суммирование числовых значений списка
